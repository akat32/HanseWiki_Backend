import { Request, Response, NextFunction } from "express";
import * as randomString from 'randomstring'

const people = {
    newPeople: async (req: Request, res: Response)=>{
        
    }
}
export { people }